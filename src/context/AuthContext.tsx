/**
 * @deprecated Importar desde `@/features/auth/context/AuthContext` en código nuevo.
 * Este barrel se mantiene para compatibilidad con imports existentes durante la migración.
 */
export { AuthProvider, useAuth } from '@/features/auth/context/AuthContext';
