/**
 * @deprecated Importar desde `@/features/auth/context/AuthContext` en c�digo nuevo.
 * Este barrel se mantiene para compatibilidad con imports existentes durante la migraci�n.
 */
export { AuthProvider, useAuth } from '@/features/auth/context/AuthContext';
